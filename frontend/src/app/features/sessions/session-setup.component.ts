import { Component } from '@angular/core';

@Component({
  selector: 'app-session-setup',
  standalone: true,
  template: '<p style="color:var(--color-text-faint)">Session setup — coming soon</p>',
})
export class SessionSetupComponent {}
